import React from 'react';

const Rotate: React.FC = () => {
    return (
        <div>
            <h1>Rotate</h1>
            <p>options for rotate with button to trigger an api call</p>
        </div>
    );
};

export default Rotate;
