import React from 'react';
import { useImageContext } from '../../context/ImageContext';

const FetchTask: React.FC = (task) => {

  return (
    <div>
    
    </div>
  );
};

export default FetchTask;
